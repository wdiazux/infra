# emby HTTPRoute (Gateway API)
#
# Routes traffic via Cilium Gateway API with TLS termination.
# Uses shared Gateway at 10.10.2.20 for TLS termination.
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: emby
  namespace: media
  labels:
    app.kubernetes.io/name: emby
spec:
  parentRefs:
    - name: cilium-gateway
      namespace: kube-system
  hostnames:
    - emby.home-infra.net
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: emby
          port: 80
