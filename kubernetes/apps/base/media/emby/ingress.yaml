# Emby Ingress
#
# Provides HTTPS access via emby.home-infra.net
# Uses wildcard certificate from cert-manager.
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: emby
  namespace: media
  labels:
    app.kubernetes.io/name: emby
    app.kubernetes.io/part-of: media
  annotations:
    kubernetes.io/ingress.class: cilium
spec:
  tls:
    - hosts:
        - emby.home-infra.net
      secretName: wildcard-home-infra-net-tls
  rules:
    - host: emby.home-infra.net
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: emby
                port:
                  number: 80
