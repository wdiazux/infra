# Homepage LoadBalancer Service
#
# Static IP via Cilium L2 LoadBalancer
apiVersion: v1
kind: Service
metadata:
  name: homepage
  namespace: tools
  labels:
    app.kubernetes.io/name: homepage
  annotations:
    io.cilium/lb-ipam-ips: "${IP_HOMEPAGE}"
spec:
  type: LoadBalancer
  loadBalancerIP: "${IP_HOMEPAGE}"
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: homepage
