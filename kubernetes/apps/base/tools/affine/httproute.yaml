# affine HTTPRoute (Gateway API)
#
# Parallel to existing Ingress for migration testing.
# Uses shared Gateway at 10.10.2.19 for TLS termination.
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: affine
  namespace: tools
  labels:
    app.kubernetes.io/name: affine
spec:
  parentRefs:
    - name: cilium-gateway
      namespace: kube-system
  hostnames:
    - affine.home-infra.net
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: affine
          port: 80
