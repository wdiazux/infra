# IT-Tools LoadBalancer Service
#
# Static IP via Cilium L2 LoadBalancer
apiVersion: v1
kind: Service
metadata:
  name: it-tools
  namespace: tools
  labels:
    app.kubernetes.io/name: it-tools
  annotations:
    io.cilium/lb-ipam-ips: "${IP_ITTOOLS}"
spec:
  type: LoadBalancer
  loadBalancerIP: "${IP_ITTOOLS}"
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: it-tools
