# ntfy Service
#
# ClusterIP - accessed via Gateway API (https://ntfy.home-infra.net)
apiVersion: v1
kind: Service
metadata:
  name: ntfy
  namespace: tools
  labels:
    app.kubernetes.io/name: ntfy
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: ntfy
