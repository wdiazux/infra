# ComfyUI Ingress
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: comfyui
  namespace: ai
  labels:
    app.kubernetes.io/name: comfyui
  annotations:
    kubernetes.io/ingress.class: cilium
spec:
  tls:
    - hosts:
        - comfy.home-infra.net
      secretName: wildcard-home-infra-net-tls
  rules:
    - host: comfy.home-infra.net
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: comfyui
                port:
                  number: 80
