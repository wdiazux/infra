# comfyui HTTPRoute (Gateway API)
#
# Routes traffic via Cilium Gateway API with TLS termination.
# Uses shared Gateway at 10.10.2.20 for TLS termination.
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: comfyui
  namespace: ai
  labels:
    app.kubernetes.io/name: comfyui
spec:
  parentRefs:
    - name: cilium-gateway
      namespace: kube-system
  hostnames:
    - comfy.home-infra.net
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: comfyui
          port: 80
