# Prowlarr LoadBalancer Service
#
# Static IP via Cilium L2 LoadBalancer
apiVersion: v1
kind: Service
metadata:
  name: prowlarr
  namespace: arr-stack
  labels:
    app.kubernetes.io/name: prowlarr
    app.kubernetes.io/part-of: arr-stack
  annotations:
    io.cilium/lb-ipam-ips: "${IP_PROWLARR}"
spec:
  type: LoadBalancer
  loadBalancerIP: "${IP_PROWLARR}"
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: prowlarr
