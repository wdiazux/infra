# Prowlarr Service
#
# ClusterIP - accessed via Gateway API (https://prowlarr.home-infra.net)
apiVersion: v1
kind: Service
metadata:
  name: prowlarr
  namespace: arr-stack
  labels:
    app.kubernetes.io/name: prowlarr
    app.kubernetes.io/part-of: arr-stack
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: prowlarr
