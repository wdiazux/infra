# SABnzbd Service
#
# ClusterIP - accessed via Gateway API (https://sabnzbd.home-infra.net)
apiVersion: v1
kind: Service
metadata:
  name: sabnzbd
  namespace: arr-stack
  labels:
    app.kubernetes.io/name: sabnzbd
    app.kubernetes.io/part-of: arr-stack
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: sabnzbd
