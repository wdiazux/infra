# Zitadel PostgreSQL Kustomization
#
# PostgreSQL 17 database for Zitadel identity provider.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - secret.enc.yaml
  - certificate.yaml
  - statefulset.yaml
  - service.yaml
