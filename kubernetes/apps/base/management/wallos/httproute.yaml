# wallos HTTPRoute (Gateway API)
#
# Routes traffic via Cilium Gateway API with TLS termination.
# Uses shared Gateway at 10.10.2.20 for TLS termination.
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: wallos
  namespace: management
  labels:
    app.kubernetes.io/name: wallos
spec:
  parentRefs:
    - name: cilium-gateway
      namespace: kube-system
  hostnames:
    - wallos.home-infra.net
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: wallos
          port: 80
