# ControlD DNS Domain Definitions
# Auto-generated by generate-dns-config.py
# Source: HTTPRoute manifests in kubernetes/
# Generated: 2026-01-25T02:46:56Z
#
# DO NOT EDIT MANUALLY - changes will be overwritten
# To customize: edit HTTPRoutes or the generator script
#
# Routing architecture:
#   *.home-infra.net, *.reynoza.org -> 10.10.2.20 (Cilium Gateway API, HTTPS)
#   Static resources (proxmox, nas) -> direct IPs

domains:
  # ==========================================================================
  # Infrastructure
  # ==========================================================================
  - name: gitops
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Gateway API

  - name: hubble
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Gateway API

  - name: longhorn
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Gateway API

  - name: nas
    ip: 10.10.2.5
    suffixes: [home-infra.net]
    # Direct IP access

  - name: proxmox
    ip: 10.10.2.2
    suffixes: [home-infra.net]
    # Direct IP access

  - name: pve
    ip: 10.10.2.2
    suffixes: [home-infra.net]
    # Direct IP access

  # ==========================================================================
  # Ai
  # ==========================================================================
  - name: chat
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Gateway API

  - name: comfy
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Gateway API

  # ==========================================================================
  # Applications
  # ==========================================================================
  - name: affine
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Gateway API

  - name: attic
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Gateway API

  - name: auth
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Gateway API

  - name: copyparty
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Gateway API

  - name: emby
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Gateway API

  - name: git
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Gateway API

  - name: grafana
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Gateway API

  - name: hass
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Gateway API

  - name: home
    ip: 10.10.2.20
    fqdn: home-infra.net
    # HTTPS via Gateway API

  - name: metrics
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Gateway API

  - name: minio
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Gateway API

  - name: music
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Gateway API

  - name: n8n
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Gateway API

  - name: ntfy
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Gateway API

  - name: obico
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Gateway API

  - name: paperless
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Gateway API

  - name: photos
    ip: 10.10.2.20
    suffixes: [reynoza.org]
    # HTTPS via Gateway API

  - name: tools
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Gateway API

  - name: wallos
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Gateway API

  # ==========================================================================
  # Arr Stack
  # ==========================================================================
  - name: bazarr
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Gateway API

  - name: prowlarr
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Gateway API

  - name: qbittorrent
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Gateway API

  - name: radarr
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Gateway API

  - name: sabnzbd
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Gateway API

  - name: sonarr
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Gateway API
