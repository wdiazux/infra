# ControlD DNS Domain Definitions
# Auto-generated by generate-dns-config.py
# Source: kubernetes/infrastructure/cluster-vars/cluster-vars.yaml
# Generated: 2026-01-25T01:32:44Z
#
# DO NOT EDIT MANUALLY - changes will be overwritten
# To customize: edit cluster-vars.yaml or the generator script
#
# Routing architecture:
#   *.home-infra.net, *.reynoza.org -> 10.10.2.20 (Cilium Gateway API, HTTPS)

domains:
  # ==========================================================================
  # Infrastructure
  # ==========================================================================
  - name: hubble
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Ingress

  - name: longhorn
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Ingress

  - name: git
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Ingress

  - name: gitops
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Ingress

  - name: minio
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Ingress

  - name: auth
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Ingress

  - name: proxmox
    ip: 10.10.2.20
    aliases: [pve]
    suffixes: [home-infra.net]
    # HTTPS via Ingress

  - name: ingress
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Ingress

  - name: nas
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Ingress

  # ==========================================================================
  # Ai
  # ==========================================================================
  - name: ollama
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Ingress

  - name: chat
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Ingress

  - name: comfy
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Ingress

  # ==========================================================================
  # Applications
  # ==========================================================================
  - name: home
    ip: 10.10.2.20
    fqdn: home-infra.net
    # HTTPS via Ingress

  - name: photos
    ip: 10.10.2.20
    suffixes: [reynoza.org]
    # HTTPS via Ingress

  - name: grafana
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Ingress

  - name: metrics
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Ingress

  - name: hass
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Ingress

  - name: n8n
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Ingress

  - name: obico
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Ingress

  - name: attic
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Ingress

  - name: emby
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Ingress

  - name: music
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Ingress

  - name: tools
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Ingress

  - name: affine
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Ingress

  - name: wallos
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Ingress

  - name: ntfy
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Ingress

  - name: paperless
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Ingress

  - name: files
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Ingress

  # ==========================================================================
  # Arr Stack
  # ==========================================================================
  - name: sabnzbd
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Ingress

  - name: qbittorrent
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Ingress

  - name: prowlarr
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Ingress

  - name: radarr
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Ingress

  - name: sonarr
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Ingress

  - name: bazarr
    ip: 10.10.2.20
    suffixes: [home-infra.net]
    # HTTPS via Ingress
